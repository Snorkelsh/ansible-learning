---

- hosts: all
  become: true
  tasks:


  - name: "Update all"
    package:
      update_only: yes
      update_cache: yes

- hosts: virt1
  become: true
  tasks:

  - name: "install neofetch on virt1"
    package:
      name: 
        - neofetch
      state: latest

- hosts: virt2
  become: true
  tasks:

  - name: "install cowsay on virt2"
    package:
      name:
        - cowsay
      state: latest
